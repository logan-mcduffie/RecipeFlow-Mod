# Project
mod_id=recipeflow
mod_name=RecipeFlow
mod_version=1.0.0
mod_group=com.recipeflow.mod

# Dependencies (legacy - now in libs.versions.toml)
gson_version=2.10.1

# Gradle settings
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
